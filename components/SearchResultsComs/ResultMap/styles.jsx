import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  markerImage: {
    width: 40,
    height: 50,
    resizeMode: "contain",
  },
});

export default styles;
